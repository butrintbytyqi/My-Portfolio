# Overrides the Netlify UI build settings, which still point at the
# old CRA "build" directory. Vite outputs to "dist".
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
